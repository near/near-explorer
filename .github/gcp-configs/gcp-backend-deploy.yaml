steps:
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      [
        "run",
        "deploy",
        "${_CLOUD_RUN_SERVICE}",
        "--image",
        "${_IMAGE}:${_PR_NUMBER}",
        "--region",
        "${_REGION}",
        "--tag",
        "pr-${_PR_NUMBER}",
      ]
