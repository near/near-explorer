steps:
  # Build the BackEnd
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-f",
        "backend/Dockerfile",
        "-t",
        "${_IMAGE}:${_PR_NUMBER}",
        ".",
      ]
  # Push Backend to Artifact Registry
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "${_IMAGE}:${_PR_NUMBER}"]
