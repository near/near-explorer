import { Row } from "react-bootstrap";

import Pagination from "../utils/Pagination";

const TransactionsPaginationFooter = props => (
  <Row>
    <Pagination />
  </Row>
);

export default TransactionsPaginationFooter;
